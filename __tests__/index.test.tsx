import { render, screen } from '@testing-library/react'
import Index from './../pages/index'
import { mockProps } from './mock'

describe('Index', () => {

  const props = mockProps;

  it('renders appropriately', async () => {
    /*const wrapper = render(<Index {...props} />)
    try {
      expect(screen.getByText("Cargando")).toBeCalled()
    } catch (error) { }*/
  })

})